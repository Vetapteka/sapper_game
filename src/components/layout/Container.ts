import styled from 'styled-components';
import { CONTENT_WIDTH } from '../../styles/global';

export const Container = styled.div`
    margin: 0 auto;
    padding: 10px;
    max-width: ${CONTENT_WIDTH};
`;
